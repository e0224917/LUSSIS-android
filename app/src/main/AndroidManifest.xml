<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sa45team7.lussis">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <meta-data
        android:name="com.google.android.gms.vision.DEPENDENCIES"
        android:value="barcode" />

    <application
        android:name=".LUSSISApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.ScanQRActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.BaseActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.GenerateQRActivity"
            android:label="Disbursement Detail"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.BaseActivity" />
        <activity
            android:name=".activities.BaseActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.PendingReqDetailActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.BaseActivity" />
        <activity
            android:name=".activities.StationeryDetailActivity"
            android:label="Stationery Detail"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.BaseActivity" />
    </application>

</manifest>